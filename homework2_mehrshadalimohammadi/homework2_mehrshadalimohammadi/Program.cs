using homework2_mehrshadalimohammadi.Task4;

namespace homework2_mehrshadalimohammadi
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
            Task3:

            int num1 = int.Parse(Console.ReadLine());
            int num2 = int.Parse(Console.ReadLine());
            int num3 = int.Parse(Console.ReadLine());
            var test = new Statistics();
            test.AnalyzeNumbers(num1, ref num2, out num3);

            Console.WriteLine($"number1 : {num1}");
            Console.WriteLine($"number2 : {num2}");
            Console.WriteLine($"number3 : {num3}");
            */


            ////////////////////////////////////////////////////////////////////////////////////////////////////
            
            
            /*
            //Task4:

            Console.Write("pls insert size of the arry: ");
            int size = int.Parse(Console.ReadLine());

            int[] arr = new int[size];

            for (int i = 0; i < size; i++)
            {
                Console.Write($"pls insert number{i + 1} of the arry :");
                arr[i] = int.Parse(Console.ReadLine());
            }

            ArrayAnalyzer analyzer = new ArrayAnalyzer();

            int max = 0;
            double average = 0;

            analyzer.AnalyzeArray(arr, ref max, out average);

            Console.WriteLine($"max number is: {max}");
            Console.WriteLine($"average is: {average}");
            */
        }
    }
}
